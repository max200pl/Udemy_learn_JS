/**
 * * Array.from(document.querySelectorAll("h1")) -> return new arr [h1]
 * * from attached to Array constructor -> [1,2,3].form -> not work
 * * .from() -> method is in the Array name space. Array.from()
*/

Array.from(document.querySelectorAll("h1"))
// * Array.from() is basically just a simple function.
// * Is function attach to the Array constructor
Number.parseFloat("12");